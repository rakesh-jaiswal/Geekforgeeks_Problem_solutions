
string firstRepChar(string s)
{
    //code here.
    unordered_map<char,int>m;
    string t;
    for(int i=0;i<s.size();i++){
        if(m.find(s[i])!=m.end())
        return t+s[i];
        else m[s[i]]++;
    }
    return "-1";
}
